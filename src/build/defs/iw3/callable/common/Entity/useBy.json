{
	"usage": "<entity> useBy(<using entity>)",
	"description": [
		"Uses the entity with the passed in entity as the activator."
	],
	"params": [
		{
			"name": "using entity",
			"types": ["Entity"],
			"description": [
				"The entity that is using the other entity."
			],
			"optional": false
		}
	],
	"example": [
		"level.armoredCar.turrent useBy(level.player);"
	]
}
