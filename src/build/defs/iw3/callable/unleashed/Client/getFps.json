{
	"usage": "<client> getFps()",
	"description": ["Get a client's FPS as perceived by the server (can be different from what the client sees)."],
	"return": {
		"type": "int"
	},
	"example": ["fps = self getFps();"]
}
