{
	"usage": "strTokByLen(<string>, <length>)",
	"description": [
		"Counts the number of characters of a given string and tokenizes it so that it will never exceed the given limit. This function tries to separate the string so that words remain complete."
	],
	"params": [
		{
			"name": "string",
			"type": "string",
			"description": ["String to tokenize."],
			"optional": false
		},
		{
			"name": "length",
			"type": "int",
			"description": ["Maximum amount of characters in a segment."],
			"optional": false
		}
	],
	"return": {
		"type": "Array"
	},
	"example": ["tok = strTokByLen(\"Hello world!\", 6);"]
}
